<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/card_bg_light" />
    <corners android:radius="16dp" /> <!-- More pronounced rounding -->
    <stroke android:width="1dp" android:color="@color/divider_cute" /> <!-- Subtle border -->
    <padding
        android:left="16dp"
        android:top="16dp"
        android:right="16dp"
        android:bottom="16dp" />
</shape>